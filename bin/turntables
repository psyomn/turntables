#!/usr/bin/env ruby
require 'turntables'
include Turntables

if ARGV.count > 0
  turntable = Turntable.new
  turntable.register(ARGV[0])
  turntable.make!
else
  puts "Usage: "
  puts "  turtables <repo-dir>"
end
